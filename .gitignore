node_modules
dist
.vite

# Every example keeps its workspace API key in an ignored local file.
.env
.env.local
.env.*.local

.DS_Store
._*
*.log
.idea
.vscode

# Dev-only SDK tarballs packed from the local animated-waffle checkout.
.sdk-local
